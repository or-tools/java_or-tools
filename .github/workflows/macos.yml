name: MacOS

on: [push, pull_request]

jobs:
  # Building using the github runner environement directly.
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: Check java
      run: java -version
    - name: Check maven
      run: mvn --version
    - name: Install or-tools
      run: |
        mkdir repo;
        cd repo;
        wget "https://github.com/google/or-tools/releases/download/v8.0/java_osx.zip";
        unzip java_osx.zip;
        unzip -j ortools-java-8.0.8283.jar META-INF/maven/com.google.ortools/ortools-java/pom.xml;
        mvn org.apache.maven.plugins:maven-install-plugin:2.5.2:install-file -Dfile=ortools-java-8.0.8283.jar -DpomFile=pom.xml;
        mvn org.apache.maven.plugins:maven-install-plugin:2.5.2:install-file -Dfile=ortools-darwin-8.0.8283.jar;
    - name: Compile
      run: mvn compile
    - name: Test
      run: mvn exec:java -Dexec.mainClass="org.mizux.ortools.Test"
